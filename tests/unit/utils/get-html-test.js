import getHtml from 'ember-rtl/utils/get-html';
import { module, test } from 'qunit';

module('Unit | Utility | get html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getHtml();
  assert.ok(result);
});
